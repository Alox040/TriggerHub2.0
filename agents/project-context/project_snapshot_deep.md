> Veraltet in Teilen seit 2026-03-11. Aussagen zu `clipExporter.ts`, zum alten `agent/`-Baum und zu leeren Metadaten sind nicht mehr vollständig aktuell. Diese Datei nur als historische Tiefenanalyse verwenden, nicht als primäre Source of Truth.

File: docs/PROJECT_DEEP_SNAPSHOT.md
Location: /docs/PROJECT_DEEP_SNAPSHOT.md
Generated by: Super Snapshot Agent
Generated at: 2026-03-11T16:36:06+01:00

# Deep Project Snapshot

## System Topology

- Desktop runtime: `src/` + `electron/`
- Website runtime: `website/`
- Workflow automation: `scripts/` + `.github/workflows/`
- Agent orchestration layer: `agents/`
- Product/release metadata plane: `project-meta/` + `releases/` + `project-context/`
- Supporting workspaces: `design/`, `marketing/`, `tools/exe-builder/`

## Desktop Runtime Deep View

### Composition Root and Lifecycle

`createAppModuleContainer()` in `src/app/bootstrap.ts` constructs all runtime modules and returns a `RuntimeContainer` with `start()` and `stop()` lifecycle methods.

Runtime `start()` sequence:
1. `appController.start()` — hotkey/window manager initialization
2. `obsService.connect()` — OBS connection established
3. `spotifyService.play()` — Spotify session activation
4. `clipService.startCapture()` — clip capture buffer opened
5. Service state flags (`obs`, `spotify`, `clip`) set to `true`
6. `pluginRegistry.activateAll()` — plugins activated with full context injection

Runtime `stop()` sequence:
1. `triggerEngine.destroy()` — clears all subscriptions
2. `pluginRegistry.deactivateAll()`
3. `obsService.disconnect()`
4. `spotifyService.pause()`
5. `appController.stop()`
6. All service state flags set to `false`

Default boot seeding (`seedDefaultCoreData`):
- Registers one macro: `macro-default-scene` (OBS switchScene to "Main")
- Registers one trigger: `trigger-main-scene` bound to `OBS_CONNECTED`, calls `macro.run`

### Event -> Trigger -> Action -> Macro Flow

1. Event published via `InMemoryEventBus`.
2. `TriggerEngine` resolves matching triggers by topic from `TriggerGraph`.
3. Trigger conditions evaluated (`evaluateCondition`).
4. Actions dispatched through `TriggerExecutor.toDispatcher()`.
5. Executor handlers call service actions or `macro.run`/`macro` (by-name lookup).
6. `MacroEngine` executes typed steps; supports recursion via `macro_call` with depth tracking.
7. Supported macro step types: `delay`, `service_call`, `plugin_action`, `macro_call`, `conditional`, `parallel`, `sequence`.
8. Conditional steps evaluate `equals`, `not_equals`, `greater_than`, `less_than`, `contains`, `exists` against `ctx.variables`.
9. Completion events re-published (`TRIGGER_EXECUTED`, `MACRO_COMPLETED`).
10. UI subscribes via facade for dashboard state refresh.

### Core Subsystems

- **Event bus** (`src/core/event-bus/`): subscription, one-shot handlers, wildcard topics (`:*`), `InMemoryEventBus` implementation.
- **Trigger domain** (`src/core/trigger-engine/`): `TriggerEngine`, `TriggerGraph`, `TriggerExecutor`, `TriggerExecutorError`, invariant checks, event-indexed graph, dispatch error collection.
- **Macro domain** (`src/core/macro-system/`): `MacroEngine`, `MacroRunner`, step invariant checks, execution context/variables, depth-aware recursion guard.
- **App control** (`src/core/app-control/`): `AppController`, `HotkeyManager`, `WindowManager` abstraction exposed via controller.

### Service Layer

- **OBS service** (`src/services/obs-service/`): in-memory and HTTP transports behind typed `contracts.ts`, `obsClient.ts`, `obsActions.ts`.
- **Spotify service** (`src/services/spotify-service/`): same pattern — in-memory and HTTP transports.
- **Clip service** (`src/services/clip-service/`): in-memory buffer processing, pluggable exporters. Has browser-safe (`clipExporter.browser.ts`) and Node-safe (`clipExporter.node.ts`) split plus platform entry (`node.ts`, `clipExporter.ts`).
- **Shared reliability** (`src/services/shared/reliability.ts`): timeout/retry wrapper with `ServiceOperationError`.
- Known boundary issue: `clipExporter.ts` (the non-suffixed entry) imports Node APIs (`node:fs/promises`, `node:path`) and may be pulled into browser build path depending on import resolution.

### Plugin Model

- `PluginRegistry` (`src/plugins/pluginRegistry.ts`): register/unregister/list and activation lifecycle.
- `createPluginRegistryWithDefaults()` bootstraps the example plugin at startup.
- Plugin activation receives `{ appController, triggerEngine, macroEngine, eventBus, actionRegistry }`.
- Example plugin in `src/plugins/example-plugin/`.

### Facade and Read Model

- `TriggerHubAppFacade` (`src/app/facade.ts`): thin read facade over trigger engine, macro engine, and service state for UI consumption.
- `readModel.ts` (`src/app/readModel.ts`): derived read-only view of app state.
- `container.ts` (`src/app/container.ts`): `AppModuleContainer` interface definition.

## Website Deep View

### Access and Routing

Route definitions are centralized in `website/src/app/routing/routeManifest.ts`.

Defined routes and groups:

| Path | Group | Base Policy |
|---|---|---|
| `/access` | `public_auth` | public |
| `/` | `public_marketing` | public (owner-only in `private_prelaunch`) |
| `/features` | `public_marketing` | public (owner-only in `private_prelaunch`) |
| `/pricing` | `public_marketing` | public (owner-only in `private_prelaunch`) |
| `/about` | `public_marketing` | public (owner-only in `private_prelaunch`) |
| `/login` | `public_auth` | public |
| `/signup` | `public_auth` | public except `private_prelaunch` (owner-only) |
| `/app` | `protected_product` | protected (owner-only in `private_prelaunch`) |
| `/dashboard` | `protected_product` | protected (mode-specific overrides) |
| `/profile` | `protected_product` | protected (mode-specific overrides) |
| `/settings` | `protected_product` | protected (mode-specific overrides) |
| `/forbidden` | `system` | public |
| `/logout` | `system` | protected (owner-only in `private_prelaunch`) |
| `/internal` | `system` | protected (owner-only in `private_prelaunch`) |

Access modes: `private_prelaunch`, `invite_only`, `public_product`. Resolved via `getResolvedRoutePolicy(path, mode, options)` which applies `modeOverrides` over `basePolicy`. Signup can be disabled via `signupEnabled: false` option.

### Auth and Identity — Two-Layer Gate Architecture

The website auth system consists of two independent provider layers introduced in the v0.1.1 development cycle:

#### Layer 1: PrelaunchGateProvider (`website/src/app/providers/PrelaunchGateProvider.tsx`)

- Active only when `appAccessMode === 'private_prelaunch'`.
- On mount, calls `GET /api/prelaunch-gate/me` to verify existing gate session.
- Exposes `authorize(accessKey)` which calls `POST /api/prelaunch-gate/login`.
- Gate state: `isGateEnabled`, `isGateOpen`, `isGateInitializing`, `gateUnavailableReason`.
- `AccessPage` (`website/src/pages/AccessPage.tsx`) renders the gate entry form; reads `appAccessMode` from `runtimeConfig` and calls `authorize` then navigates to `nextPath`.

#### Layer 2: AuthProvider (`website/src/app/providers/AuthProvider.tsx`)

- Depends on `PrelaunchGateProvider` via `usePrelaunchGate()`.
- Waits for gate to resolve before initializing owner session.
- If gate is enabled and closed: sets session to null, does not call backend.
- Session initialization: calls `GET /api/auth/me`; maps `BackendSessionSnapshot` -> `AuthSession` via `createServerBackedSession()`.
- Login: calls `POST /api/auth/login`.
- Logout: calls `POST /api/auth/logout`.
- Exposes `AuthContextValue`: `session`, `identity`, `isAuthenticated`, `isInitializing`, `isAuthAvailable`, `authUnavailableReason`, `login`, `logout`.
- `AuthError` (`website/src/modules/auth/errors.ts`): typed error class for auth failures.

#### Session Types

`AuthSession` (client-side) fields: `sessionId`, `guardId`, `userId`, `role`, `email`, `identityCreatedAt`, `createdAt`, `expiresAt`.

`BackendSessionSnapshot` fields validated in `backendSession.ts`: `userId`, `role` (must be `'owner'`), `email`, `issuedAt` (ISO date), `expiresAt` (ISO date), `lastAuthenticatedAt` (ISO date), `sessionVersion` (integer >= 1).

`createServerBackedSession()` validates the snapshot, checks `expiresAt > issuedAt`, and maps to `AuthSession` with a freshly generated `crypto.randomUUID()` as both `sessionId` and `guardId`.

#### Deleted Auth Modules (Breaking Change in v0.1.1)

The following modules were removed as part of the OW-003 security hardening and auth rebuild:
- `website/src/modules/auth/authService.ts`
- `website/src/modules/auth/backendAuthProvider.ts`
- `website/src/modules/auth/ownerAuthProvider.ts`
- `website/src/modules/auth/passwordHashing.ts`

These were replaced by `backendSession.ts`, `errors.ts`, `backendAuthContract.ts` (referenced but not directly read), and the two-provider architecture.

### Auth Identity

`AuthIdentity` (from `access-control/types`): `userId`, `role`, `email`, `createdAt`. Derived from `AuthSession` via `toIdentity()` in `AuthProvider`.

`UserRole` type is defined in `website/src/modules/access-control/types`.

### Profile and Data

- Profile runtime and validation modules present in `website/src/modules/profile`.
- Generated website content pipeline writes JSON to `website/src/content/generated`.
- Current content quality is constrained because many source metadata files in `project-meta/` are empty.

## Workflow and Deployment Deep View

- Local release pipeline: `scripts/run-release.ts` -> `scripts/release-orchestrator.ts`.
- Context sync script: `scripts/context-sync.ts` (new in v0.1.1, run via `npm run context:sync`).
- Generate AI context: `scripts/generate-ai-context.ts`.
- Feature change watcher: `scripts/feature-change-watcher.ts`.
- CI release pipeline: validates, syncs, builds Windows artifacts, publishes GitHub release, optionally triggers Vercel.
- CI website sync: regenerates content on metadata changes and auto-commits.
- Desktop distribution: `electron-builder` targeting Windows NSIS x64, artifact name `TriggerHubSetup.exe`, publish target GitHub (`Alox040/Triggerhub`).
- Security verification: `website/scripts/verify-prelaunch-security.mjs` runs as `prebuild` step in website build.
- `ts-node` config in root `package.json` points to `tsconfig.scripts.json` with `esm: true`, `transpileOnly: true`.

## Agent System Deep View

Canonical agent tree is `agents/`. The old `agent/` directory (flat, non-canonical) is scheduled for deletion (shown as `D` in git status).

### Current Agent Inventory (`agents/**/*.md`)

**Orchestration:**
- `agents/master-orchestrator.md` — root orchestrator prompt (German), directs agents in `agents/core/`
- `agents/core/00-agent-rules.md` — global agent rules

**Core Agents:**
- `agents/core/01-orchestrator.md`
- `agents/core/02-product.md`
- `agents/core/03-architecture.md`
- `agents/core/04-implementation.md`
- `agents/core/05-uiux.md`
- `agents/core/06-qa.md`
- `agents/core/07-ops.md`
- `agents/core/08-docs.md`
- `agents/core/09-autoupdate.md`
- `agents/core/10-marketing-ops.md` (new)
- `agents/core/11-debug-agent.md` (new)
- `agents/core/12-review-agent.md` (new)
- `agents/core/40-security-audit-agent.md`

**Workflow Agents:**
- `agents/10-super-snapshot-agent.md`
- `agents/15-context-snapshot-agent.md` (new)
- `agents/20-content-sync-agent.md`
- `agents/25-context-sync-agent.md`
- `agents/40-release-agent.md`

**Optional:**
- `agents/optional/10-snapshot.md`

**System Templates:**
- `agents/system/context-template.md`
- `agents/system/decision-log-template.md`
- `agents/system/handoff-template.md`
- `agents/system/review-template.md`
- `agents/system/task-template.md`

**Project Context:**
- `agents/project-context/active-tasks.md`
- `agents/project-context/architecture-overview.md`
- `agents/project-context/changelog.md`
- `agents/project-context/decision-log.md`
- `agents/project-context/design-guidelines.md`
- `agents/project-context/known-issues.md`
- `agents/project-context/marketing-ops-role-review.md` (new)
- `agents/project-context/product-overview.md`
- `agents/project-context/project_snapshot.md`
- `agents/project-context/project_snapshot_deep.md`

Master orchestrator routing rules: Security Audit Agent is mandatory for security-trigger tasks. Standard workflow order: Architecture/Product -> Implementation -> QA -> Security Audit (if triggered) -> Implementation/Ops -> QA -> Docs -> Release.

## Test Suite

### Test Files (16 total)

| File | Domain |
|---|---|
| `src/tests/app-container.test.ts` | Container wiring |
| `src/tests/app-facade.test.ts` | Facade read model |
| `src/tests/core-macro.test.ts` | Macro engine |
| `src/tests/core-trigger.test.ts` | Trigger engine |
| `src/tests/event-bus.test.ts` | Event bus |
| `src/tests/plugins.test.ts` | Plugin registry |
| `src/tests/services.test.ts` | Service adapters |
| `src/tests/trigger-executor.test.ts` | Trigger executor |
| `src/tests/trigger-graph.test.ts` | Trigger graph |
| `src/tests/website-auth-v1.test.ts` | Website auth v1 |
| `src/tests/website-browser-guards.test.ts` | Browser route guards |
| `src/tests/website-owner-only-access.test.ts` | Owner-only access verification |
| `src/tests/website-prelaunch-gate.test.ts` | Prelaunch gate |
| `src/tests/website-profile-v1.test.ts` | Profile module |
| `src/tests/app-context.test.tsx` | App context (React) |
| `src/tests/ui-dashboard.test.tsx` | UI dashboard (React) |

## Current Build / Test / Typecheck Status

### Tests: PASS

```
Test Files: 16 passed (16)
Tests:      121 passed (121)
Duration:   1.48s
```

All 121 tests pass across 16 test files. Increase from 106 tests in previous snapshot (+15 tests, reflecting new auth/gate/access test coverage).

### Typecheck: PASS

```
> tsc --noEmit
(no output, exit 0)
```

Typecheck now passes cleanly. Previously failing errors (missing PNG asset module declarations, `ImportMeta.env` typing, and related issues) have been resolved.

### Website Build: PASS

```
vite v6.3.5 building for production...
2029 modules transformed.
dist/index.html              0.44 kB  | gzip: 0.29 kB
dist/assets/index.css      138.66 kB  | gzip: 19.73 kB
dist/assets/index.js       310.16 kB  | gzip: 97.98 kB
built in 2.15s
```

Prebuild security check (`verify-prelaunch-security.mjs`) runs and passes before Vite build.

### Root Build (`npm run build`): PASS

```
vite v6.4.1 building for production...
85 modules transformed.
dist/index.html                 0.40 kB  | gzip: 0.27 kB
dist/assets/index-CD1wmCDz.css  0.48 kB  | gzip: 0.29 kB
dist/assets/index-D4mTE33J.js 170.01 kB  | gzip: 53.92 kB
built in 524ms
```

Root build now passes in this run. This reduces the previous clip exporter browser-boundary uncertainty, though boundary discipline should still be enforced in future changes.

## Development Debt and Structural Risks

1. **Build status is environment-sensitive**: Root and website builds pass in this run, but both required unrestricted process spawning (sandboxed runs failed with `spawn EPERM`). CI or restricted environments must be validated separately.

2. **Metadata completeness**: Many files in `project-meta/product`, `project-meta/features`, `project-meta/integrations`, and `project-meta/status` remain empty or near-empty. Generated content pipeline output quality is low.

3. **Auto-update not implemented**: `agents/core/09-autoupdate.md` agent spec exists; no runtime auto-update implementation detected in `src/`.

4. **No durable persistence**: All trigger/macro data is seeded in-memory at startup. There is no persistence layer for user-defined triggers or macros across restarts.

5. **Documentation drift risk**: Large working tree (80+ modified/deleted/untracked files on `release/v0.1.1-prep` branch) means many doc files are out of sync with current code state. Snapshot-style docs must be regenerated after each significant change.

6. **Security enforcement gap**: Security audit is policy-mandated by orchestrator rules but is not a blocking CI stage. Policy depends on manual orchestrator discipline.

7. **Unstaged agent tree cleanup**: The old `agent/` directory (36 files) is pending deletion in the working tree but not yet committed. Until committed, both `agent/` and `agents/` exist simultaneously.

8. **Large in-flight refactor footprint**: The working tree still contains many staged/unstaged modifications and untracked files. Merge discipline and targeted validation remain important to avoid drift.

## Recent Changes (git log)

```
f3b276a fix(release): bump version to 0.1.1, add context:sync script and ts-node config
3f78853 release: prepare v0.1.1 (OW-003, workflow hardening, branch trigger compatibility)
b4949b2 release: prepare v0.1.1 (OW-003, workflow hardening, branch trigger compatibility)
a668479 Initial commit created by release workflow repair
```

### Key Changes Since Previous Snapshot

- Version bumped to `0.1.1` (was `0.1.0` at last deep snapshot).
- OW-003 security hardening: removed `authService.ts`, `backendAuthProvider.ts`, `ownerAuthProvider.ts`, `passwordHashing.ts`; replaced with `backendSession.ts`, `errors.ts`, two-provider gate architecture (`PrelaunchGateProvider` + `AuthProvider`).
- `PrelaunchGateProvider` and `AccessPage` introduced for two-layer prelaunch access flow.
- Typecheck errors resolved (previously 4 errors, now 0).
- Test count grew from 106 to 121 (+15), new test files for `website-owner-only-access`, `website-prelaunch-gate`, and `website-browser-guards`.
- New scripts: `scripts/context-sync.ts`, `scripts/generate-ai-context.ts`.
- New agents: `agents/core/11-debug-agent.md`, `agents/core/12-review-agent.md`, `agents/15-context-snapshot-agent.md`, `agents/core/10-marketing-ops.md`.
- `tsconfig.scripts.json` added for `ts-node` ESM script execution.
- Agent tree migration: canonical `agents/` consolidation in progress; old `agent/` pending deletion.
- Clip service split into browser/node entry points (untracked, in working tree).

## Verification Signals (this run)

- Tests: PASS (121 tests across 16 files).
- Typecheck: PASS (0 errors).
- Root build: PASS (85 modules, 524ms).
- Website build: PASS (2029 modules, 2.15s).
