File: docs/PROJECT_SNAPSHOT.md  
Location: /docs/PROJECT_SNAPSHOT.md  
Generated by: Super Snapshot Agent  
Generated at: 2026-03-11T01:19:24+01:00

# Project Snapshot

## Project Overview
- Project: TriggerHub 2.0 (`package.json`)
- Purpose: desktop automation hub for creator workflows (event -> trigger -> action and macro orchestration) with a separate public website.
- Primary targets: Windows desktop app (Electron + React) and Vite React website (`website/`).
- Delivery model: local release scripts + GitHub Actions + optional Vercel deploy hook.

## Simplified Repository Map
- Core app: `src/`, `electron/`
- Website app: `website/`
- Agent system: `agents/` and mirrored `agent/agents/`
- Release and sync workflows: `.github/workflows/`, `scripts/`, `releases/`
- Product metadata: `project-meta/`, `project-context/`
- Tooling and utility workspaces: `tools/`, `marketing/`, `design/`
- Quality surface: `src/tests/`, `tests/`, root scripts (`typecheck`, `test`, `build`)

## Architecture Snapshot
- Composition root: `src/app/bootstrap.ts` wires event bus, trigger engine, macro engine, services, plugins, and facade.
- Event system: in-memory bus with direct and wildcard topics (`src/core/event-bus`).
- Trigger system: trigger graph + condition evaluation + action dispatch (`src/core/trigger-engine`).
- Macro system: structured steps including conditional, sequence, and parallel execution (`src/core/macro-system`).
- Service layer: OBS, Spotify, Clip adapters with reliability policy and optional HTTP transports (`src/services`).
- Plugin layer: registry lifecycle (`register/list/activate/deactivate`) with default example plugin bootstrap.
- UI layer: desktop dashboard in `src/ui`; website routing/auth/profile stack in `website/src`.

## Workflow Snapshot
- Local release command: `npm run release` -> validate -> desktop artifact build.
- Validation command: `npm run release:validate` executes content sync, checks, typecheck, tests, builds, metadata validation.
- CI release workflow: `.github/workflows/release.yml` (validate, build artifacts, publish release, trigger deploy hook).
- CI website sync workflow: `.github/workflows/website-update.yml` (regenerate and commit website content).
- Content generation: `scripts/generate-website-content.ts` from `project-meta/*` and `releases/changelog-source.json`.

## Agent Snapshot
- Active primary tree: `agents/*`
- Mirrored legacy tree: `agent/agents/*`
- Key runtime roles detected: orchestrator, architecture, implementation, QA, ops, docs, security audit, snapshot, content sync, release, auto-update, super snapshot.
- Notable split: legacy-only `agent/agents/core/10-marketing-ops.md`.

## Current Health (verified in this run)
- `npm run test`: PASS (14 files, 106 tests).
- `npm run typecheck`: FAIL (website asset module declarations, `ImportMeta.env` typing).
- `npm run build` (root): FAIL (browser build imports Node APIs in `src/services/clip-service/clipExporter.ts`).
- `npm --prefix website run build`: PASS.

## High-Level Status
- Implemented: core engine stack, service adapters, plugin registry, desktop packaging/release scaffolding, website auth/access/profile foundation.
- Partial: content metadata population, several product UI areas, release-readiness quality gates.
- Main risks: dual agent trees, empty/near-empty public metadata sources, unresolved typecheck/build blockers.
